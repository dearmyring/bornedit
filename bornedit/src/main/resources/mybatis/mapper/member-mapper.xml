<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<insert id="join" parameterType="MemberDto">
		insert into member 
		    (member_email, member_pw, member_nick, member_introduce, member_offer) 
		values
		    (#{memberEmail}, #{memberPw}, #{memberNick}, #{memberIntroduce}, #{memberOffer});
	</insert>
	
	<select id="getEmail" parameterType="String" resultType="MemberDto">
		select member_email from member where member_email = #{memberEmail}
	</select>
	
	<select id="getNick" parameterType="String" resultType="MemberDto">
		select member_nick from member where member_nick = #{memberNick}
	</select>
</mapper>